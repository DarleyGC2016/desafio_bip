import { AtivoRespostaPipe } from './ativo-resposta.pipe';

describe('AtivoRespostaPipe', () => {
  it('create an instance', () => {
    const pipe = new AtivoRespostaPipe();
    expect(pipe).toBeTruthy();
  });
});
